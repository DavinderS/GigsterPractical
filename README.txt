Need Maven to run this. Once installed just open StartServer.cmd.
You also need to host the JavaScript/HTML on an HTTP server because of the cross domain security issues. 
You can do this by doing: angular-http-server -p 8081 in the command prompt, but you'll need to download this from https://www.npmjs.com/package/angular-http-server